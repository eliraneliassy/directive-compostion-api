import { ChangeSizeDirective } from './change-size.directive';

describe('ChangeSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeSizeDirective();
    expect(directive).toBeTruthy();
  });
});
