import { MyMenuTriggerDirective } from './my-menu-trigger.directive';

describe('MyMenuTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new MyMenuTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
